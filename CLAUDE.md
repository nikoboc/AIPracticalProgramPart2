# CLAUDE.md

このファイルは、このリポジトリで作業するすべてのセッションが最初に読む前提情報。

## このリポジトリの目的

**とある「AI実践力育成プログラム」の第2部カリキュラムを作成するためのリポジトリ。** 扱うテーマは **n8n**（ノーコードのワークフロー自動化プラットフォーム）。

既存の講義動画がすでに存在しており、**それをブラッシュアップして新しいカリキュラムを作る**のが本リポジトリの主目的。

### 作業の流れ（これまでに完了していること）

1. 既存の講義動画（全9本・計約1時間58分）を文字起こしする → `Transcripts/`
2. 文字起こしの表記ゆれを統一する → `unify_terms.py` / `Transcripts/表記ゆれ統一表.md`
3. 文字起こしから、各動画の説明内容・画面操作手順・入力値を整理する → `第2部カリキュラム/既存動画/`
4. **既存動画は古く、n8n を含む各種サービスの最新仕様と一致しない部分がある。** 最新仕様を調査し、齟齬を修正した版を作る → `第2部カリキュラム/最新仕様反映/`

## ディレクトリ構成

| パス | 内容 |
|---|---|
| `Transcripts/` | 既存動画の文字起こし（`.txt` / `.srt`）と表記ゆれ統一表 |
| `第1部カリキュラム/` | 第1部（AI活用の基礎、全10章）のカリキュラム。**第2部の前提**として参照する |
| `第2部カリキュラム/既存動画/` | 文字起こしから整理した、**既存動画の説明内容そのまま**。現行仕様への補正は入れていない |
| `第2部カリキュラム/最新仕様反映/` | 上記を **2026-09-21 時点の最新仕様に合わせて修正した版**。各ファイル冒頭に「現行仕様との差分サマリ」がある |
| `transcribe.py` | 文字起こしスクリプト |
| `unify_terms.py` | 表記ゆれ一括変換スクリプト |

## 既存動画の構成（全9本）

「**なんちゃって ChatGPT**」を1本のワークフローとして作り始め、段階的に機能を足していく構成。各回は**欠点を体験させてから、それを解消する機能を追加する**流れで統一されている。

```
① オリエンテーション              → 講座のゴール、n8n とは
② 基本操作                        → アカウント作成、管理画面、キャンバス
③ トリガー + AI Agent             → 頭脳がなくエラー
④ + Chat Model（OpenRouter）      → 会話できるが記憶がない
⑤ + Memory / System Message       → 記憶できる／役割・口調を制御できる
⑥ + Wikipedia / Gmail ツール      → 外部情報・外部操作。順番制御が課題に
⑦ + HTTP Request（Tavily）        → 専用ノードがないサービスとも連携できる
⑧ サブエージェントへ分離           → 別キャンバス化して管理性・安定性を上げる
⑨ フレームワークと演習             → 自力でオリジナルを作れる状態へ
```

## 作業時に必ず守ること

### 1. 既存動画版は「動画のまま」を保つ

`第2部カリキュラム/既存動画/` は**文字起こしに忠実な記録**であり、**現行仕様への補正を入れてはいけない**。
仕様の補正は `第2部カリキュラム/最新仕様反映/` 側で行う。

### 2. n8n の仕様は変わり続ける

**n8n は更新が非常に速い**（2025-12 に 2.0 → 2026-09 時点で 2.40）。
仕様に言及するときは、**必ず公式ドキュメント（docs.n8n.io）で当日の内容を確認**し、
**調査日と n8n のバージョンをファイルに明記する**こと。記憶や過去の調査結果をそのまま使わない。

- ドキュメントは URL に `.md` を付けると Markdown で取得できる（例：`https://docs.n8n.io/build/understand-workflows/save-and-publish-workflows.md`）
- 全ページの一覧は `https://docs.n8n.io/sitemap.md`

### 3. 現時点で最大の仕様変更：Save ボタンの廃止

既存動画は「**n8n には自動保存がない。変更のたびに Save を押せ**」というメッセージを②以降ほぼ毎回繰り返しているが、**現行の n8n にこの操作は存在しない**。

- n8n 2.4（2026-01-13）で**オートセーブが導入され、Save ボタン・Ctrl+S・コマンドバーの save はすべて削除**された。
- 編集内容はすべて**ドラフト**として自動保存される。**本番反映は `Publish` ボタン**（`Shift` + `P`）。
- 旧 Active / Inactive トグルは **Publish / Unpublish** に置き換わった。

カリキュラムの記述でこれに触れる場合は、必ず現行の仕様に合わせること。
詳細は [`第2部カリキュラム/最新仕様反映/README.md`](第2部カリキュラム/最新仕様反映/README.md)。

### 4. 要再確認リスト

`第2部カリキュラム/最新仕様反映/README.md` の「要再確認リスト」に、**確定できなかった項目・変わりやすい項目**をまとめてある。収録・実施の直前には必ずここを確認する。

## Git

- コミットは既定ブランチ（`main`）へ直接行う。指示がない限り、コミットのために新しいブランチを作らない。
- コミットメッセージは**日本語の行を先に、英語の行を下に**書く（件名・本文の各行とも）。
- ファイル名の日本語は **NFC で統一**する（macOS の既定は NFD になりやすいので注意。過去に一度統一済み）。

## ドキュメントの言語

- 日本語を `README.md`、英語を `README.en.md` のように**別ファイルに分ける**。1つのファイル内で行ごとに併記しない。
- ただし**カリキュラム本体（`第2部カリキュラム/` 配下）は日本語のみ**でよい。受講者向けの教材であるため。
- コード中のコメントは日本語・英語を併記する（日本語の行が先）。
